diff --git a/pyproject.toml b/pyproject.toml
index c0e5267..b211ecd 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -41,7 +41,6 @@ dependencies = [
   "hypothesis[zoneinfo]>=6.103.4,<7; python_version == '3.8'",
   "hypothesis_jsonschema>=0.23.1,<0.24",
   "jsonschema[format]>=4.18.0,<5.0",
-  "junit-xml>=1.9,<2.0",
   "pyrate-limiter>=2.10,<4.0",
   "pytest-subtests>=0.2.1,<0.15.0",
   "pytest>=4.6.4,<9",
diff --git a/src/schemathesis/cli/__init__.py b/src/schemathesis/cli/__init__.py
index 7b3115f..5c7d533 100644
--- a/src/schemathesis/cli/__init__.py
+++ b/src/schemathesis/cli/__init__.py
@@ -52,7 +52,6 @@ from .constants import DEFAULT_WORKERS, MAX_WORKERS, MIN_WORKERS, HealthCheck, P
 from .context import ExecutionContext, FileReportContext, ServiceReportContext
 from .debug import DebugOutputHandler
 from .handlers import EventHandler
-from .junitxml import JunitXMLHandler
 from .options import CsvChoice, CsvEnumChoice, CsvListChoice, CustomHelpMessageChoice, OptionalInt
 from .sanitization import SanitizationHandler
 
@@ -1723,9 +1722,6 @@ def execute(
                 telemetry=telemetry,
             )
         )
-    if junit_xml is not None:
-        _open_file(junit_xml)
-        handlers.append(JunitXMLHandler(junit_xml))
     if debug_output_file is not None:
         _open_file(debug_output_file)
         handlers.append(DebugOutputHandler(debug_output_file))
@@ -1750,7 +1746,7 @@ def execute(
         wait_for_schema=wait_for_schema,
         validate_schema=validate_schema,
         cassette_path=cassette_path.name if cassette_path is not None else None,
-        junit_xml_file=junit_xml.name if junit_xml is not None else None,
+        junit_xml_file=None,
         verbosity=verbosity,
         code_sample_style=code_sample_style,
         report=report_context,
diff --git a/test/cli/output/test_default.py b/test/cli/output/test_default.py
index 12c848a..866f62e 100644
--- a/test/cli/output/test_default.py
+++ b/test/cli/output/test_default.py
@@ -20,6 +20,8 @@ from schemathesis.runner.events import Finished, InternalError
 from schemathesis.runner.serialization import SerializedTestResult
 
 
+@pytest.mark.skip("JUnitXML support removed")
+
 @pytest.fixture(autouse=True)
 def click_context():
     """Add terminal colors to the output in tests."""
diff --git a/test/cli/test_junitxml.py b/test/cli/test_junitxml.py
index e5a8634..75f8e05 100644
--- a/test/cli/test_junitxml.py
+++ b/test/cli/test_junitxml.py
@@ -6,6 +6,8 @@ from xml.etree import ElementTree
 import pytest
 from _pytest.main import ExitCode
 
+pytestmark = pytest.mark.skip("JUnitXML support removed")
+
 
 @pytest.mark.operations("success")
 def test_junitxml_option(cli, schema_url, hypothesis_max_examples, tmp_path):
